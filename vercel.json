{
  "version": 2,
  "builds": [
    { "src": "api/**/*.js", "use": "@now/node" },
    { "src": "public/**/*", "use": "@now/static" }
  ],
  "routes": [{ "src": "/api/(.*)", "dest": "/api/$1" }, { "src": "/(.*)", "dest": "/$1" }]
}
